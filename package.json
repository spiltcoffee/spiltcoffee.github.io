{
  "name": "spiltcoffee.github.io",
  "private": true,
  "scripts": {
    "postdfm": "node postdfm.js",
    "stopwatch": "node stopwatch.js",
    "prebuild": "yarn install --frozen-lockfile",
    "build": "yarn postdfm && yarn stopwatch"
  },
  "devDependencies": {
    "execa": "^2.0.3",
    "fs-extra": "^8.1.0"
  }
}
